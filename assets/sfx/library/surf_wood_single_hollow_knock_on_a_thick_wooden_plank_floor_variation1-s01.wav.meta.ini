; Physical Ragdoll Sounds - sfx metadata
; Written by the testbench when this file was imported or edited. Everything under
; [Measured] is what the importer measured; Name and Disabled are yours.
; Deleting this file loses the measurements, the import date and the mute - not
; the sound.

[Sfx]
Name = surf_wood_single_hollow_knock_on_a_thick_wooden_plank_floor_variation1-s01
; imported 2026-08-23 17:36
Imported = 1787499413
Loops = 0
Disabled = 0

[Format]
SampleRate = 48000
Channels = 1
BitsPerSample = 16
DurationMs = 335.2

; Measured the way tools/sfx.py measures them, so these numbers can be compared
; against the tables in Slots.md directly. Tilt is (sub+low)/2 - (high+air)/2 in dB:
; positive is bass-led, negative is bright, and it is the discriminator that works.
[Measured]
PeakDb = -1.51
LeadInMs = 2.0
UsableMs = 320.0
Decay20Ms = 58.0
CentroidHz = 383
TiltDb = 52.46
DcOffset = 0.00006
LoMidTransients = 5
SeamDb = 34.24
SteadyDb = 10.95
GrainsPerSec = 17.9

; The technical rule-outs, on tools/triage_batch.py's thresholds: hiss under 30 dB
; down, a squared-off waveform, a second contact riding under the hero, and a top
; octave that is not there. Hash is over the samples, so the same sound under two
; names has the same one.
[Technical]
NoiseFloorDb = 99.0
ClipPct = 0.000
ClipRuns = 0
Contacts = 2
SatelliteDb = -30.9
SatelliteAtMs = 250
TopOctaveDb = -8.1
ContentHash = 924d781f9fbef5ba

; Slots this suits, best first. A suggestion the browser sorts on, never a rule -
; anything can be assigned to anything.
[Suggested]
Slots = imp_sub, settle_rest, head_impact

; What the file breaks, as `code|detail` - `code|detail|1` for the one class that
; cannot be played at all, and `code|detail|dead` for the ones that play and cannot
; be repaired. None of these stop it being assigned.
[Warnings]
Warning = satellite|a second contact +250 ms from the hero, -31 dB under it.\n\n03-Asset-Status.md §7 found one of these in 39 takes out of 100 - usually a bright debris wash the generator bolted on. Left in, it lands on top of whatever the engine schedules next and reads as a flam; on an impact layer it collides with `imp_sub`'s own arrival at +55-75 ms.\n\nFix: cut it. `python tools/sfx.py split <file>` cuts at the 46 ms contact floor, and tools/triage_batch.py drops satellites at exactly this 20 dB threshold and records every one.
Warning = duplicate|the same samples as `surf_wood_single_hollow_knock_on_a_thick_wooden_plank_floor_variation1-s01_2`, which is already in the library. Two names for one sound, and a slot that plays both draws the same file twice as often as it looks like it does - a variant that is not a variant.\n\nDelete one of them. Nothing is lost: they are identical.|dead
