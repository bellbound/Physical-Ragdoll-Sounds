; Physical Ragdoll Sounds - sfx metadata
; Written by the testbench when this file was imported or edited. Everything under
; [Measured] is what the importer measured; Name and Disabled are yours.
; Deleting this file loses the measurements, the import date and the mute - not
; the sound.

[Sfx]
Name = unsorted_naked_body_sliding_along_ground_dense_continuous_variation3-long
; imported 2026-08-23 17:36
Imported = 1787499413
Loops = 0
Disabled = 0

[Format]
SampleRate = 48000
Channels = 1
BitsPerSample = 16
DurationMs = 9978.0

; Measured the way tools/sfx.py measures them, so these numbers can be compared
; against the tables in Slots.md directly. Tilt is (sub+low)/2 - (high+air)/2 in dB:
; positive is bass-led, negative is bright, and it is the discriminator that works.
[Measured]
PeakDb = -1.51
LeadInMs = 16.0
UsableMs = 9958.0
Decay20Ms = 178.0
CentroidHz = 7921
TiltDb = -21.26
DcOffset = 0.00000
LoMidTransients = 587
SeamDb = 19.54
SteadyDb = 3.82
GrainsPerSec = 73.6

; The technical rule-outs, on tools/triage_batch.py's thresholds: hiss under 30 dB
; down, a squared-off waveform, a second contact riding under the hero, and a top
; octave that is not there. Hash is over the samples, so the same sound under two
; names has the same one.
[Technical]
NoiseFloorDb = 99.0
ClipPct = 0.000
ClipRuns = 0
Contacts = 0
SatelliteDb = 0.0
SatelliteAtMs = 0
TopOctaveDb = -12.5
ContentHash = 813268b2eff65453

; Slots this suits, best first. A suggestion the browser sorts on, never a rule -
; anything can be assigned to anything.
[Suggested]
Slots = 

; What the file breaks, as `code|detail` - `code|detail|1` for the one class that
; cannot be played at all, and `code|detail|dead` for the ones that play and cannot
; be repaired. None of these stop it being assigned.
[Warnings]
Warning = very long|10.0 s and it does not measure as a texture. Either it has a baked room tail or it is several sounds in one file.\n\nA long file is not wrong on its own: Slots.md §6 wants textures cut both ways, and the only `scrape_loop` to pass the 2026-08-23 batch was a 9.9 s long cut. It is wrong in a one-shot slot, which plays the whole of it.\n\nFix: `sfx.py make` windows it.
Warning = duplicate|the same samples as `unsorted_naked_body_sliding_along_ground_dense_continuous_variation3-long_2`, which is already in the library. Two names for one sound, and a slot that plays both draws the same file twice as often as it looks like it does - a variant that is not a variant.\n\nDelete one of them. Nothing is lost: they are identical.|dead
